# Core runtime — always required.
flask>=3.0
watchdog>=4.0
pyyaml>=6.0
numpy>=1.26
mcp>=1.0,<2   # 2.x renamed FastMCP -> MCPServer and changed the tool API
# Codex zstd-compresses rollouts older than 7 days in place; without this
# every Codex session silently drops out of the browser after a week.
zstandard>=0.22

# Semantic search (optional but recommended). Pulls in torch (~2 GB).
# Skip with `./install.sh --lite`; search then falls back to keyword/full-text.
# sentence-transformers>=2.7   <- installed by install.sh unless --lite
